# 123. 买卖股票的最佳时机 III

Tags: 动态规划
URL: https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/

# 随想录

原始的买卖股票最佳时期，dp数组的含义是当前持有或是不持有股票，所得到的最大收益。

所谓不持有，不是指的在当前轮一定把股票卖了。

![image.png](123%20%E4%B9%B0%E5%8D%96%E8%82%A1%E7%A5%A8%E7%9A%84%E6%9C%80%E4%BD%B3%E6%97%B6%E6%9C%BA%20III/image.png)

## 第二次持有股票可以从第一次不持有股票推来

如果说第一次不持有股票这个状态，一直都是0，那么说明这个序列是降序的，因此一定是亏的。不然就必然有一次卖的行为发生，即该状态值大于0，因此对于第一次不持有股票这个状态大于0的**首个位置i**，发生了一次卖的操作。

```cpp
// 版本一
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        if (prices.size() == 0) return 0;
        vector<vector<int>> dp(prices.size(), vector<int>(5, 0));
        dp[0][1] = -prices[0];
        dp[0][3] = -prices[0];
        for (int i = 1; i < prices.size(); i++) {
            dp[i][0] = dp[i - 1][0];
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] - prices[i]);
            dp[i][2] = max(dp[i - 1][2], dp[i - 1][1] + prices[i]);
            dp[i][3] = max(dp[i - 1][3], dp[i - 1][2] - prices[i]);
            dp[i][4] = max(dp[i - 1][4], dp[i - 1][3] + prices[i]);
        }
        return dp[prices.size() - 1][4];
    }
};
```