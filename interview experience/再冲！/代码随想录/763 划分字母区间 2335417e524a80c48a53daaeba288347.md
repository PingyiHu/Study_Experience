# 763. 划分字母区间

Tags: 贪心
URL: https://leetcode.cn/problems/partition-labels/description/

# 我的方法

先把26个字母所占的区间统计好

然后找最大的不重叠区间

```cpp
class Solution {
public:
    static bool cmp(const vector<int>& a, const vector<int>& b) {
        return a[0] < b[0]; // 改为左边界排序
    }

    vector<int> partitionLabels(string s) {
        vector<vector<int>> vv(26, vector<int>());
        // 构建数组
        for (int i = 0; i < s.size(); i++) {
            int idx = s[i] - 'a';
            if (vv[idx].size() == 0) {
                vv[idx].push_back(i);
                vv[idx].push_back(i);
            } else {
                vv[idx][1] = i;
            }
        }
        vector<vector<int>> intervals;
        for (int i = 0; i < vv.size(); i++) {
            if (vv[i].size() != 0)
                intervals.push_back(vv[i]);
        }
        sort(intervals.begin(), intervals.end(), cmp);

        vector<int> result;
        int end = intervals[0][1];
        int pre = 0;
        for (int i = 1; i < intervals.size(); i++) {
            if (intervals[i][0] < end) {
                end = max(intervals[i][1], end);
            } else {
                int tmp = end - pre + 1;
                result.push_back(tmp);
                pre = end + 1;
                end = intervals[i][1];
            }
        }
        int tmp = end - pre + 1;
        result.push_back(tmp);
        return result;
    }
};
```

# 随想录

每次找到最大边界，当遍历到边界时，说明是分割点。

![image.png](763%20%E5%88%92%E5%88%86%E5%AD%97%E6%AF%8D%E5%8C%BA%E9%97%B4/image.png)

```cpp
class Solution {
public:
    vector<int> partitionLabels(string S) {
        int hash[27] = {0}; // i为字符，hash[i]为字符出现的最后位置
        for (int i = 0; i < S.size(); i++) { // 统计每一个字符最后出现的位置
            hash[S[i] - 'a'] = i;
        }
        vector<int> result;
        int left = 0;
        int right = 0;
        for (int i = 0; i < S.size(); i++) {
            right = max(right, hash[S[i] - 'a']); // 找到字符出现的最远边界
            if (i == right) {
                result.push_back(right - left + 1);
                left = i + 1;
            }
        }
        return result;
    }
};
```