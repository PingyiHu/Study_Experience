# 144. 二叉树的遍历

Tags: 二叉树
URL: https://leetcode.cn/problems/binary-tree-preorder-traversal/description/

# 递归

- 确定函数的参数和返回值
- 确定迭代终止条件
- 确定单层递归逻辑

# 前序迭代

```cpp
class Solution {
public:
    vector<int> preorderTraversal(TreeNode* root) {
        stack<TreeNode*> st;
        vector<int> result;
        if (root == NULL) return result;
        st.push(root);
        while (!st.empty()) {
            TreeNode* node = st.top();                       // 中
            st.pop();
            result.push_back(node->val);
            if (node->right) st.push(node->right);           // 右（空节点不入栈）
            if (node->left) st.push(node->left);             // 左（空节点不入栈）
        }
        return result;
    }
};
```

# 后序迭代利用前序翻转

- 前序是中左右，栈内是右左
- 如果把栈内改为左右，出栈后就是中右左
- 再进行逆序就是后序的左右中，即后序

# 中序迭代

中序的访问和处理不是同时发生的。

需要用一个栈保存已经访问的节点，当到达叶子节点之后，反着访问直到栈空或是指针为NULL

```
classSolution {
public:
    vector<int>inorderTraversal(TreeNode* root) {
        vector<int> result;
        stack<TreeNode*> st;
        TreeNode* cur = root;
while (cur != NULL || !st.empty()) {
if (cur != NULL) {// 指针来访问节点，访问到最底层
                st.push(cur);// 将访问的节点放进栈
                cur = cur->left;// 左
            }else {
                cur = st.top();// 从栈里弹出的数据，就是要处理的数据（放进result数组里的数据）
                st.pop();
                result.push_back(cur->val);// 中
                cur = cur->right;// 右
            }
        }
return result;
    }
};

```

# 统一迭代法

随想录说是每个节点访问两次：

1. 为自己和儿子在栈中安排位次
2. 真正进行处理

其实这个流程和408拆散再访问的流程是一致的：

![image.png](144%20%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/image.png)

中序分解流程，分解之前节点状态是false，之后是true：

5

4 5 6

1 4 2 5 6

后序统一迭代法：

```cpp
class Solution {
public:
    vector<int> postorderTraversal(TreeNode* root) {
        vector<int> result;
        stack<pair<TreeNode*, bool>> st;
        if (!root)
            return result;
        st.push(make_pair(root, false));
        while (!st.empty()) {
            auto node = st.top().first;
            auto flag = st.top().second;
            st.pop();
            if (flag) {
                result.push_back(node->val);
            } else {
                st.push(make_pair(node, true));
                if (node->right)
                    st.push(make_pair(node->right, false));
                if (node->left)
                    st.push(make_pair(node->left, false));
            }
        }
        return result;
    }
};
```