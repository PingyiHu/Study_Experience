# 718. 最长重复子数组

Tags: 动态规划, 没做出来
URL: https://leetcode.cn/problems/maximum-length-of-repeated-subarray/

# 随想录

dp数组额外往外扩了两维，方便处理

dp数组的意思是以nums1[i]，nums2[j]为结尾的数组的公共数组

```python
class Solution(object):
    def findLength(self, nums1, nums2):
        """
        :type nums1: List[int]
        :type nums2: List[int]
        :rtype: int
        """
        dp = [[0 for j in range(len(nums2) + 1)] for i in range(len(nums1) + 1)]
        max_length = 0
        for i in range(1, len(nums1) + 1):
            for j in range(1, len(nums2) + 1):
                if nums1[i - 1] == nums2[j - 1]:
                    dp[i][j] = dp[i - 1][j - 1] + 1
                if dp[i][j] > max_length:
                    max_length = dp[i][j]

        return max_length

```